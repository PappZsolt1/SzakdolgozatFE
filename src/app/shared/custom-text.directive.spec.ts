import { CustomTextDirective } from './custom-text.directive';

describe('CustomTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTextDirective();
    expect(directive).toBeTruthy();
  });
});
